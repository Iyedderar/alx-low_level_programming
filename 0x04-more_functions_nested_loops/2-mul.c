#include"main.h"
/**
 * _isdigit - checks for uppercase characters
 * Return: the product of the two argument
 * @c: first operand
 * @d: second operand
 */

int mul(int c, int d)
{
	return (c*d);
}

